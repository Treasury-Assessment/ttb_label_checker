# ============================================================================
# Backend Environment Variables (.env.example)
# ============================================================================
# All configuration values are OPTIONAL with sensible defaults.
# This file is only needed if you want to customize settings.
#
# Copy this file to .env if you want to customize configuration:
#   cp .env.example .env
#
# NEVER commit the .env file to version control
# ============================================================================

# ----------------------------------------------------------------------------
# OCR Configuration (All Optional - Defaults Provided)
# ----------------------------------------------------------------------------

# Minimum confidence threshold for OCR text detection
# Range: 0.0 (accept all) to 1.0 (require perfect match)
# Default: 0.7
# Recommended: 0.6-0.8 for production use
OCR_CONFIDENCE_THRESHOLD=0.7

# Maximum allowed image file size in bytes
# Default: 10485760 (10 MB)
# Examples:
#   5 MB  = 5242880
#   10 MB = 10485760
#   20 MB = 20971520
# MAX_IMAGE_SIZE=10485760

# Supported image formats (comma-separated, case-insensitive)
# Default: JPEG,PNG,WEBP,HEIC
# Note: OCR quality may vary by format (JPEG and PNG recommended)
# SUPPORTED_FORMATS=JPEG,PNG,WEBP,HEIC

# Example customizations:
# - Higher security (smaller images, fewer formats):
#   MAX_IMAGE_SIZE=5242880
#   SUPPORTED_FORMATS=JPEG,PNG
#
# - More permissive (larger images, more formats):
#   MAX_IMAGE_SIZE=20971520
#   SUPPORTED_FORMATS=JPEG,PNG,WEBP,HEIC,TIFF

# ----------------------------------------------------------------------------
# NOT NEEDED FOR LOCAL EMULATOR TESTING
# ----------------------------------------------------------------------------
# The following are auto-detected:
#
# ❌ GOOGLE_CLOUD_PROJECT_ID - Auto-detected from .firebaserc
# ❌ FIREBASE_STORAGE_BUCKET - Not used (no storage in this app)
# ❌ GOOGLE_APPLICATION_CREDENTIALS - Vision API uses ADC automatically
#
# For real Vision API calls during local development:
#   Run: gcloud auth application-default login
#   OR: export GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# ----------------------------------------------------------------------------
# Production Deployment
# ----------------------------------------------------------------------------
# In production, Firebase auto-detects all credentials from Cloud Functions
# environment. No secrets or environment variables needed!
#
# For sensitive data, use Firebase Secrets:
#   firebase functions:secrets:set SECRET_NAME
# See: https://firebase.google.com/docs/functions/config-env#secret-manager
