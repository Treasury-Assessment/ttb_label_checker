/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  // Note: For Firebase App Hosting, we use 'standalone' instead of 'export'
  // This enables server-side rendering on Cloud Run
}

module.exports = nextConfig
